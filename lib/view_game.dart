import 'package:flutter/material.dart';

class ViewGamePage extends StatelessWidget {
  const ViewGamePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(),
    );
  }
}
